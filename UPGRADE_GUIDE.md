# 🔄 Руководство по обновлению до версии 2.0

## Что нового?

### 🚀 GitHub API Integration
Все ваши публичные репозитории теперь автоматически загружаются с GitHub!

### 🎨 Современный дизайн
Glassmorphism эффекты, новые градиенты и плавные анимации.

---

## 📋 Пошаговая инструкция

### Шаг 1: Обновите зависимости

```bash
pip install -r requirements.txt
```

Будут установлены:
- `requests` - для GitHub API
- `python-dotenv` - для переменных окружения

### Шаг 2: Создайте файл `.env`

Скопируйте `env.example` в `.env`:

**Windows:**
```bash
copy env.example .env
```

**Linux/Mac:**
```bash
cp env.example .env
```

### Шаг 3: Настройте GitHub

Откройте `.env` и измените:

```env
GITHUB_USERNAME=ваш_username
GITHUB_TOKEN=  # Опционально, но рекомендуется
```

#### Как получить токен (опционально):

1. Перейдите: https://github.com/settings/tokens
2. **Generate new token (classic)**
3. **Scopes:** выберите только `public_repo`
4. **Generate token**
5. Скопируйте токен и вставьте в `.env`:
```env
GITHUB_TOKEN=ghp_ваш_токен_здесь
```

**Зачем нужен токен?**
- Без токена: 60 запросов/час
- С токеном: 5000 запросов/час

### Шаг 4: Запустите приложение

```bash
python app.py
```

Откройте браузер: `http://localhost:5000`

### Шаг 5: Проверьте результат

Все ваши публичные репозитории должны отобразиться автоматически! 🎉

---

## 🔍 Проверка работы

### 1. Главная страница

Должны отобразиться:
- ✅ Все публичные репозитории
- ✅ Статистика (проекты, звезды, форки)
- ✅ Новый современный дизайн

### 2. Фильтры

Попробуйте:
- ✅ Фильтр по языку программирования
- ✅ Фильтр по тегам
- ✅ Поиск
- ✅ Сортировка (по дате, звездам, имени)

### 3. API

Откройте: `http://localhost:5000/api/projects`

Должен вернуться JSON со всеми проектами.

---

## 📚 Новая документация

Прочитайте новые файлы:

1. **GITHUB_INTEGRATION.md** - подробное руководство по GitHub API
2. **CHANGELOG.md** - полный список изменений

---

## 🎨 Что изменилось визуально?

### До (версия 1.0)
- Стандартный Bootstrap дизайн
- Статичные градиенты
- Простые карточки

### После (версия 2.0)
- ✨ Glassmorphism эффекты
- ✨ Анимированный фон
- ✨ Современные карточки с badge языка
- ✨ Плавные анимации
- ✨ Статистические карточки
- ✨ Улучшенная типографика (шрифт Inter)

---

## 🔧 Настройка под себя

### Изменить время кэширования

В `.env`:
```env
CACHE_TIMEOUT=1800  # 30 минут вместо 1 часа
```

### Исключить форки

В `app.py` найдите функцию `get_github_repos()` и раскомментируйте:

```python
# Пропускаем форки
if repo.get('fork'):
    continue
```

### Получить все языки проекта

В `app.py` раскомментируйте блок:

```python
# try:
#     lang_url = repo['languages_url']
#     lang_response = requests.get(lang_url, headers=headers, timeout=5)
#     ...
# except:
#     pass
```

⚠️ Внимание: это увеличит количество запросов к API!

---

## 🐛 Возможные проблемы

### Проекты не загружаются

**Причина:** Неверный username в `.env`

**Решение:**
```env
GITHUB_USERNAME=ваш_правильный_username
```

### API Rate Limit Exceeded

**Причина:** Превышен лимит запросов (60/час)

**Решение:** Добавьте токен в `.env`

### Старые данные

**Причина:** Кэш не обновился

**Решение:** Откройте `http://localhost:5000/api/refresh`

---

## 🔄 Откат к версии 1.0

Если что-то пошло не так:

### 1. Откатите app.py

Восстановите старый код из git:
```bash
git checkout HEAD~1 app.py
```

### 2. Удалите новые зависимости

Удалите из `requirements.txt`:
```
requests==2.31.0
python-dotenv==1.0.0
```

### 3. Переустановите зависимости

```bash
pip install -r requirements.txt
```

---

## 📦 Миграция локальных проектов

Ваши локальные проекты (`projects/*/info.json`) продолжают работать!

### Приоритеты:

1. **Локальные проекты** переопределяют GitHub данные
2. **GitHub проекты** дополняют коллекцию

### Пример:

Если у вас есть локальный `projects/my-project/info.json` и репозиторий `my-project` на GitHub - будут использованы данные из локального файла.

---

## ✅ Чеклист обновления

- [ ] Установлены новые зависимости (`pip install -r requirements.txt`)
- [ ] Создан файл `.env` с `GITHUB_USERNAME`
- [ ] (Опционально) Добавлен `GITHUB_TOKEN`
- [ ] Запущено приложение (`python app.py`)
- [ ] Проверена главная страница
- [ ] Проверены фильтры
- [ ] Прочитана документация `GITHUB_INTEGRATION.md`
- [ ] Добавлены описания репозиториям на GitHub
- [ ] Добавлены topics (теги) на GitHub

---

## 🎉 Готово!

Теперь у вас:
- ✅ Автоматическая синхронизация с GitHub
- ✅ Современный дизайн
- ✅ Поддержка всех языков программирования
- ✅ Статистика и фильтры

### Следующие шаги:

1. **Улучшите репозитории на GitHub:**
   - Добавьте описания
   - Добавьте topics
   - Получайте звезды! ⭐

2. **Деплойте на хостинг:**
   - Render.com
   - Railway.app
   - Heroku

3. **Поделитесь с миром:**
   - Разместите ссылку в резюме
   - Покажите работодателям
   - Добавьте в соцсети

---

**Вопросы?** Читайте:
- `GITHUB_INTEGRATION.md` - подробное руководство
- `FAQ.md` - часто задаваемые вопросы
- `CHANGELOG.md` - полный список изменений

**Удачи! 🚀**

