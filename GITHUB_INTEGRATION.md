# 🔗 GitHub Integration - Руководство

## Обзор

PortfolioHub теперь автоматически загружает все ваши публичные репозитории с GitHub! Никаких JSON файлов - все проекты синхронизируются через GitHub API.

---

## ✨ Возможности

### Автоматическая синхронизация
- ✅ Все публичные репозитории загружаются автоматически
- ✅ Информация обновляется каждый час (кэширование)
- ✅ Поддержка всех языков программирования
- ✅ Автоматическое определение тегов и топиков
- ✅ Статистика: звезды, форки, язык программирования

### Что синхронизируется
- **Название репозитория** → название проекта
- **Description** → описание проекта
- **Language** → основной язык программирования
- **Topics** → теги проекта
- **Stars** → количество звезд
- **Forks** → количество форков
- **Updated At** → дата последнего обновления

---

## 🚀 Быстрый старт

### Вариант 1: Без токена (60 запросов/час)

1. Откройте `config.py` или создайте файл `.env`:
```python
GITHUB_USERNAME=dettline1
```

2. Запустите приложение:
```bash
python app.py
```

3. Готово! Все репозитории загрузятся автоматически.

### Вариант 2: С токеном (5000 запросов/час) ⭐ Рекомендуется

1. Создайте GitHub Personal Access Token:
   - Перейдите: https://github.com/settings/tokens
   - **Generate new token** → **Generate new token (classic)**
   - **Note:** `PortfolioHub API`
   - **Expiration:** Выберите срок действия
   - **Scopes:** отметьте только `public_repo`
   - Нажмите **Generate token**
   - **Скопируйте токен** (показывается один раз!)

2. Создайте файл `.env` в корне проекта:
```env
GITHUB_USERNAME=dettline1
GITHUB_TOKEN=ghp_ваш_токен_здесь
```

3. Добавьте `.env` в `.gitignore` (уже добавлен):
```
.env
```

4. Запустите:
```bash
python app.py
```

---

## ⚙️ Настройка

### Файл `config.py`

```python
# GitHub API настройки
GITHUB_USERNAME = os.environ.get('GITHUB_USERNAME') or 'dettline1'
GITHUB_TOKEN = os.environ.get('GITHUB_TOKEN')  # Опционально
GITHUB_API_URL = 'https://api.github.com'

# Кэширование (в секундах)
CACHE_TIMEOUT = int(os.environ.get('CACHE_TIMEOUT', 3600))  # 1 час
```

### Переменные окружения

Создайте файл `.env`:
```env
# GitHub настройки
GITHUB_USERNAME=ваш_username
GITHUB_TOKEN=ghp_ваш_токен  # Опционально

# Flask настройки
SECRET_KEY=случайная-строка
FLASK_ENV=development

# Кэширование (в секундах)
CACHE_TIMEOUT=3600  # 1 час
```

---

## 🎨 Фильтрация и сортировка

### Доступные фильтры

1. **По языку программирования**
   - Автоматически определяется GitHub
   - Фильтр в виде кнопок

2. **По тегам**
   - Использует GitHub Topics
   - Можно добавить вручную в настройках репозитория

3. **Поиск**
   - По названию
   - По описанию

### Сортировка

- **По дате обновления** (по умолчанию)
- **По звездам** (самые популярные первыми)
- **По имени** (алфавитный порядок)

---

## 🔄 Обновление данных

### Автоматическое обновление

Данные кэшируются на 1 час по умолчанию. Изменить можно в `.env`:
```env
CACHE_TIMEOUT=1800  # 30 минут
```

### Ручное обновление

#### Способ 1: Через браузер
```
http://localhost:5000/api/refresh
```

#### Способ 2: Кнопка на сайте
На странице "О портфолио" есть кнопка **Обновить проекты**

#### Способ 3: API запрос
```bash
curl http://localhost:5000/api/refresh
```

---

## 📊 API Endpoints

### Получить все проекты
```
GET /api/projects
```

**Пример ответа:**
```json
[
  {
    "id": "project-name",
    "name": "Project Name",
    "description": "Project description",
    "tags": ["python", "flask"],
    "link": "https://github.com/user/project",
    "stars": 42,
    "forks": 7,
    "language": "Python",
    "updated_at": "2025-01-15T10:30:00Z",
    "created_at": "2024-06-01T12:00:00Z",
    "is_fork": false,
    "homepage": "https://project.com",
    "source": "github"
  }
]
```

### Очистить кэш
```
GET /api/refresh
```

**Ответ:**
```json
{
  "status": "ok",
  "message": "Cache cleared"
}
```

---

## 🎯 Как улучшить отображение проектов

### 1. Добавьте описание репозиториям

На GitHub:
1. Откройте репозиторий
2. Нажмите ⚙️ (Settings)
3. Заполните **Description**

### 2. Добавьте Topics (теги)

На GitHub:
1. Откройте репозиторий
2. Нажмите ⚙️ рядом с **About**
3. В поле **Topics** добавьте теги (например: `python`, `web`, `api`)

### 3. Добавьте Homepage

Если у проекта есть демо:
1. Settings → **About**
2. Заполните **Website**

### 4. Получайте звезды! ⭐

Чем больше звезд - тем выше в сортировке.

---

## 🔍 Исключение репозиториев

### Вариант 1: Сделать приватным

Приватные репозитории не отображаются (даже с токеном с `public_repo` scope).

### Вариант 2: Фильтр в коде

В `app.py` найдите функцию `get_github_repos()` и добавьте фильтр:

```python
# Пропускаем форки
if repo.get('fork'):
    continue

# Пропускаем архивные
if repo.get('archived'):
    continue

# Пропускаем по имени
if repo['name'] in ['dotfiles', 'test-repo']:
    continue
```

---

## 🧩 Комбинирование с локальными проектами

Можно одновременно использовать GitHub API и локальные JSON файлы!

### Приоритеты

1. Локальные проекты имеют приоритет
2. Если есть проект с таким же `id` - используется локальный
3. GitHub проекты дополняют коллекцию

### Пример использования

**Локальный проект** (`projects/special-project/info.json`):
```json
{
  "name": "Special Project",
  "description": "Custom description, not from GitHub",
  "tags": ["custom", "important"],
  "link": "https://github.com/user/special-project",
  "stars": 999
}
```

Этот проект переопределит данные из GitHub!

---

## ⚡ Производительность

### Лимиты GitHub API

| Тип | Запросов/час | Комментарий |
|-----|--------------|-------------|
| Без токена | 60 | Достаточно для малой/средней активности |
| С токеном | 5000 | Рекомендуется для продакшна |

### Кэширование

- Данные кэшируются в памяти
- Время жизни: 1 час (настраивается)
- При перезапуске приложения кэш очищается

### Оптимизация

Приложение делает минимум запросов:
1. Один запрос для списка репозиториев (до 100 за раз)
2. Дополнительные языки НЕ запрашиваются (экономия запросов)

Раскомментируйте в `app.py` для получения всех языков:
```python
# lang_response = requests.get(lang_url, headers=headers, timeout=5)
```

---

## 🐛 Решение проблем

### Проекты не загружаются

1. **Проверьте username:**
```python
# config.py
GITHUB_USERNAME = 'ваш_правильный_username'
```

2. **Проверьте интернет соединение**

3. **Проверьте лимиты API:**
```bash
curl https://api.github.com/rate_limit
```

### Rate Limit Exceeded

Ошибка `403: API rate limit exceeded`:

**Решение:** Добавьте токен в `.env`

### Старые данные

**Решение:** Очистите кэш:
```
http://localhost:5000/api/refresh
```

### Ошибка 401 Unauthorized

Проблема с токеном:

1. Проверьте, что токен начинается с `ghp_`
2. Проверьте срок действия токена
3. Создайте новый токен

---

## 📈 Примеры использования

### Python скрипт

```python
import requests

# Получить все проекты
response = requests.get('http://localhost:5000/api/projects')
projects = response.json()

# Топ 5 по звездам
top_projects = sorted(projects, key=lambda x: x['stars'], reverse=True)[:5]

for p in top_projects:
    print(f"{p['name']}: ⭐ {p['stars']}")
```

### JavaScript

```javascript
// Получить проекты на Python
fetch('http://localhost:5000/api/projects')
  .then(r => r.json())
  .then(projects => {
    const pythonProjects = projects.filter(p => p.language === 'Python');
    console.log('Python проектов:', pythonProjects.length);
  });
```

---

## 🔐 Безопасность

### Никогда не коммитьте

❌ **НЕ добавляйте в git:**
- `.env` файл
- Токены в коде
- Секретные ключи

✅ **Безопасно:**
- Использовать переменные окружения
- `.env` в `.gitignore`
- Токены в настройках хостинга

### На продакшне

```bash
# Heroku/Render
heroku config:set GITHUB_TOKEN=ghp_...

# Railway
railway variables set GITHUB_TOKEN=ghp_...
```

---

## 📚 Дополнительно

### GitHub Topics

Рекомендуемые topics для лучшего отображения:
- Язык: `python`, `javascript`, `typescript`, `go`
- Фреймворки: `flask`, `django`, `react`, `vue`
- Категории: `api`, `bot`, `cli`, `web`, `ml`, `ai`

### Изменить название проекта

По умолчанию используется название репозитория.

Создайте локальный `info.json` для переопределения:
```json
{
  "name": "Красивое название вместо repo-name"
}
```

---

## 💡 Советы

1. **Регулярно обновляйте описания** репозиториев на GitHub
2. **Добавляйте topics** для лучшей фильтрации
3. **Используйте README** в репозиториях
4. **Звезды важны** - они влияют на сортировку
5. **Архивируйте старые** проекты на GitHub

---

## 🎉 Готово!

Теперь ваше портфолио автоматически обновляется с GitHub!

**Что дальше?**
- Добавьте описания репозиториям
- Настройте topics
- Получайте звезды! ⭐
- Деплойте на хостинг

---

**Вопросы?** Читайте FAQ.md или создавайте Issue на GitHub!

