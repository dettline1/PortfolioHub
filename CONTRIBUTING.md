# 🤝 Руководство по внесению вклада

Спасибо за интерес к проекту PortfolioHub! Мы рады любым улучшениям.

## 📋 Содержание

1. [Как помочь](#как-помочь)
2. [Настройка окружения](#настройка-окружения)
3. [Процесс разработки](#процесс-разработки)
4. [Стиль кода](#стиль-кода)
5. [Тестирование](#тестирование)
6. [Создание Pull Request](#создание-pull-request)

---

## 🎯 Как помочь

### Сообщить об ошибке

1. Проверьте, нет ли уже такой issue
2. Создайте новую issue с описанием:
   - Что произошло
   - Что ожидалось
   - Шаги для воспроизведения
   - Версия Python и ОС
   - Скриншоты (если применимо)

### Предложить улучшение

1. Создайте issue с тегом `enhancement`
2. Опишите предлагаемую функцию
3. Объясните, зачем это нужно
4. Добавьте примеры использования

### Написать код

1. Найдите issue с тегом `good first issue` или `help wanted`
2. Напишите комментарий, что берете задачу
3. Форкните репозиторий
4. Создайте ветку и напишите код
5. Отправьте Pull Request

---

## 🛠️ Настройка окружения

### 1. Форк и клонирование

```bash
# Форкните репозиторий через GitHub интерфейс

# Клонируйте свой форк
git clone https://github.com/YOUR_USERNAME/PortfolioHub.git
cd PortfolioHub

# Добавьте оригинальный репозиторий
git remote add upstream https://github.com/ORIGINAL_OWNER/PortfolioHub.git
```

### 2. Виртуальное окружение

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
# Основные зависимости
pip install -r requirements.txt

# Зависимости для разработки (если будут добавлены)
pip install -r requirements-dev.txt  # если существует
```

---

## 💻 Процесс разработки

### Создание ветки

```bash
# Обновите main
git checkout main
git pull upstream main

# Создайте новую ветку
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-description
```

Именование веток:
- `feature/` - новая функция
- `fix/` - исправление ошибки
- `docs/` - изменения в документации
- `refactor/` - рефакторинг кода
- `style/` - изменения стилей

### Разработка

1. Напишите код
2. Следуйте [стилю кода](#стиль-кода)
3. Добавьте комментарии
4. Обновите документацию
5. Проверьте работоспособность

### Коммиты

Используйте понятные сообщения:

```bash
# Хорошо ✅
git commit -m "Add search functionality to project cards"
git commit -m "Fix pagination bug on mobile devices"
git commit -m "Update README with deployment instructions"

# Плохо ❌
git commit -m "fix"
git commit -m "update"
git commit -m "changes"
```

Формат:
```
<type>: <краткое описание>

<детальное описание (опционально)>

<footer (опционально)>
```

Types:
- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование, отступы
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.д.

Пример:
```
feat: Add export to PDF functionality

- Added PDF generation using ReportLab
- Added new button in UI
- Updated documentation

Closes #123
```

---

## 📝 Стиль кода

### Python (PEP 8)

```python
# Хорошо ✅
def load_projects():
    """Загрузка всех проектов из папки projects"""
    projects = []
    projects_dir = Path('projects')
    
    for project_dir in projects_dir.iterdir():
        if project_dir.is_dir():
            # ...
    
    return projects

# Плохо ❌
def load_projects():
    projects=[]
    projectsDir=Path('projects')
    for projectDir in projectsDir.iterdir():
        if projectDir.is_dir():
            pass
    return projects
```

Правила:
- Отступы: 4 пробела (не табы)
- Максимальная длина строки: 88 символов (Black) или 79 (PEP 8)
- Docstrings для функций и классов
- Понятные имена переменных
- Одна пустая строка между функциями в классе
- Две пустые строки между классами и функциями на уровне модуля

### HTML/Jinja2

```html
<!-- Хорошо ✅ -->
<div class="card h-100 shadow-sm hover-lift">
    <div class="card-body d-flex flex-column">
        <h5 class="card-title">
            {{ project.name }}
        </h5>
        {% if project.tags %}
        <div class="mb-3">
            {% for tag in project.tags %}
            <span class="badge bg-light">{{ tag }}</span>
            {% endfor %}
        </div>
        {% endif %}
    </div>
</div>

<!-- Плохо ❌ -->
<div class="card h-100 shadow-sm hover-lift"><div class="card-body d-flex flex-column"><h5 class="card-title">{{project.name}}</h5>
{% if project.tags %}<div class="mb-3">{% for tag in project.tags %}<span class="badge bg-light">{{tag}}</span>{% endfor %}</div>{% endif %}
</div></div>
```

### CSS

```css
/* Хорошо ✅ */
.card {
    transition: all 0.3s ease;
    border: none;
    border-radius: 12px;
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
}

/* Плохо ❌ */
.card{transition:all 0.3s ease;border:none;border-radius:12px;}
.card:hover{transform:translateY(-5px);box-shadow:0 12px 24px rgba(0,0,0,0.15);}
```

---

## 🧪 Тестирование

### Перед отправкой PR

1. **Запустите приложение:**
```bash
python app.py
```

2. **Проверьте валидацию:**
```bash
python validate_projects.py
```

3. **Проверьте основные функции:**
   - [ ] Главная страница загружается
   - [ ] Проекты отображаются корректно
   - [ ] Поиск работает
   - [ ] Фильтрация по тегам работает
   - [ ] API возвращает данные
   - [ ] Генерация sitemap работает
   - [ ] Генерация README работает

4. **Проверьте адаптивность:**
   - [ ] Desktop (1920x1080)
   - [ ] Tablet (768x1024)
   - [ ] Mobile (375x667)

5. **Проверьте в разных браузерах:**
   - [ ] Chrome
   - [ ] Firefox
   - [ ] Safari
   - [ ] Edge

### Будущие тесты

Планируется добавить:
- Unit тесты (pytest)
- Интеграционные тесты
- E2E тесты (Selenium)

---

## 🚀 Создание Pull Request

### 1. Подготовка

```bash
# Убедитесь, что код работает
python app.py

# Проверьте валидацию
python validate_projects.py

# Обновите ветку
git checkout main
git pull upstream main
git checkout your-branch
git rebase main
```

### 2. Push

```bash
git push origin your-branch
```

### 3. Создание PR

1. Перейдите на GitHub
2. Нажмите "Compare & pull request"
3. Заполните шаблон:

```markdown
## Описание
Краткое описание изменений

## Тип изменения
- [ ] Исправление ошибки (bug fix)
- [ ] Новая функция (feature)
- [ ] Критическое изменение (breaking change)
- [ ] Документация (documentation)

## Как было протестировано?
Опишите, как вы тестировали изменения

## Чеклист
- [ ] Код соответствует стилю проекта
- [ ] Добавлены комментарии в сложных местах
- [ ] Обновлена документация
- [ ] Изменения не ломают существующую функциональность
- [ ] Проверено на разных устройствах/браузерах

## Скриншоты (если применимо)
```

### 4. Code Review

- Будьте открыты к обратной связи
- Отвечайте на комментарии
- Вносите запрошенные изменения
- Будьте вежливы и уважительны

---

## 🎨 Идеи для улучшений

Что можно добавить:

### Функции
- [ ] Пагинация проектов
- [ ] Сортировка (по дате, названию, популярности)
- [ ] Поддержка изображений проектов
- [ ] Темная тема
- [ ] Мультиязычность (i18n)
- [ ] Экспорт в PDF
- [ ] Интеграция с GitHub API (автоматическое получение звезд, форков)
- [ ] Аналитика просмотров
- [ ] Комментарии к проектам
- [ ] Система тегов с иерархией

### UI/UX
- [ ] Анимации при загрузке
- [ ] Скелетоны загрузки
- [ ] Модальные окна для деталей проекта
- [ ] Галерея скриншотов
- [ ] Infinite scroll
- [ ] Улучшенная мобильная навигация

### Backend
- [ ] База данных (SQLite/PostgreSQL)
- [ ] Кэширование (Redis)
- [ ] API с аутентификацией
- [ ] Webhooks
- [ ] Rate limiting
- [ ] Логирование

### DevOps
- [ ] Docker контейнер
- [ ] CI/CD pipeline
- [ ] Автоматические тесты
- [ ] Мониторинг (Sentry)

---

## 📚 Полезные ресурсы

- [Flask документация](https://flask.palletsprojects.com/)
- [Jinja2 документация](https://jinja.palletsprojects.com/)
- [Bootstrap 5 документация](https://getbootstrap.com/)
- [PEP 8 Style Guide](https://pep8.org/)
- [Git Best Practices](https://www.conventionalcommits.org/)

---

## 📞 Вопросы?

Если у вас есть вопросы:
1. Проверьте FAQ.md
2. Создайте issue с тегом `question`
3. Напишите в Discussions на GitHub

---

## 🙏 Благодарности

Спасибо всем контрибьюторам за помощь в развитии проекта!

---

**Счастливого кодинга! 🚀**

