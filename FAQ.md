# ❓ Часто задаваемые вопросы (FAQ)

## Общие вопросы

### Что такое PortfolioHub?

PortfolioHub - это автоматический генератор портфолио для Python разработчиков. Он сканирует папку с проектами, читает информацию из JSON файлов и создает красивый веб-сайт с вашими проектами.

### Нужно ли мне знать веб-разработку?

Нет! Вам нужно только добавлять JSON файлы с описанием проектов. Всё остальное делается автоматически.

### Можно ли использовать для других языков, не только Python?

Конечно! Хотя приложение написано на Python, вы можете добавлять проекты на любых языках. Просто укажите соответствующие теги в `info.json`.

---

## Установка и запуск

### Как установить зависимости?

```bash
pip install -r requirements.txt
```

### Приложение не запускается, что делать?

1. Проверьте версию Python: `python --version` (нужна 3.7+)
2. Убедитесь, что активировано виртуальное окружение
3. Переустановите зависимости: `pip install -r requirements.txt --force-reinstall`
4. Проверьте, не занят ли порт 5000: измените порт в `app.py`

### Как изменить порт?

В файле `app.py` измените последнюю строку:

```python
app.run(debug=True, host='0.0.0.0', port=8000)  # Используйте любой свободный порт
```

---

## Работа с проектами

### Как добавить новый проект?

**Способ 1 (рекомендуется):**
```bash
python add_project.py
```

**Способ 2 (вручную):**
1. Создайте папку: `projects/my-project/`
2. Создайте файл `info.json` со следующим содержимым:

```json
{
  "name": "Название проекта",
  "description": "Описание проекта (минимум 10 символов)",
  "tags": ["python", "flask"],
  "link": "https://github.com/username/repo"
}
```

### Проект не отображается на сайте, почему?

Частые причины:
1. **Неправильный JSON** - проверьте валидность: `python validate_projects.py`
2. **Неправильное имя файла** - должно быть точно `info.json`
3. **Отсутствуют обязательные поля** - проверьте наличие: name, description, tags, link
4. **Не перезагрузили страницу** - нажмите Ctrl+F5

### Можно ли использовать русский язык в JSON?

Да! Убедитесь, что файл сохранен в кодировке UTF-8.

### Какие поля обязательные в info.json?

Обязательные:
- `name` - название проекта (строка)
- `description` - описание (строка, 10-500 символов)
- `tags` - теги/технологии (массив строк)
- `link` - ссылка на GitHub (строка)

### Сколько тегов можно указать?

Рекомендуется 3-7 тегов. Технически ограничений нет, но слишком много тегов выглядит плохо.

### Можно ли добавлять изображения проекта?

В текущей версии нет, но вы можете:
1. Добавить поле `image` в `info.json`:
```json
{
  "image": "https://example.com/screenshot.png"
}
```
2. Модифицировать шаблон `templates/index.html` для отображения изображений

---

## Настройка и кастомизация

### Как изменить информацию обо мне?

Откройте `config.py` и измените блок `AUTHOR_INFO`:

```python
AUTHOR_INFO = {
    'name': 'Ваше Имя',
    'role': 'Python Developer',
    'description': 'Краткое описание о вас',
    'contacts': {
        'github': 'https://github.com/username',
        'email': 'your@email.com',
        'telegram': '@username'
    }
}
```

### Как изменить цветовую схему?

Откройте `static/style.css` и измените переменную в начале файла:

```css
:root {
    --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

Используйте генератор градиентов: [cssgradient.io](https://cssgradient.io/)

### Можно ли изменить шрифт?

Да! В `static/style.css` измените:

```css
body {
    font-family: 'Roboto', 'Arial', sans-serif;
}
```

Подключите Google Fonts в `templates/base.html`:

```html
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
```

### Как добавить новые разделы на сайт?

Модифицируйте `templates/index.html`. Добавьте новый `<section>`:

```html
<section id="skills" class="py-5">
    <div class="container">
        <h2>Мои навыки</h2>
        <!-- Ваш контент -->
    </div>
</section>
```

---

## Поиск и фильтрация

### Поиск не работает, что делать?

Поиск работает по GET параметрам. Проверьте:
1. URL должен быть вида: `/?search=query`
2. JavaScript не требуется - поиск серверный
3. Очистите кэш браузера

### Можно ли искать по тегам?

Да! Используйте фильтр по тегам или URL: `/?tag=python`

### Как работает поиск?

Поиск ищет вхождения в:
- Названии проекта (name)
- Описании проекта (description)

Поиск регистронезависимый.

---

## Деплой и продакшн

### Где лучше разместить портфолио?

Рекомендуемые платформы:
1. **Render.com** - бесплатно, автоматический деплой
2. **Railway.app** - простота, быстрый деплой
3. **Heroku** - стабильность (платно после 2022)
4. **PythonAnywhere** - бесплатно навсегда

Подробнее: `DEPLOY.md`

### Как получить свой домен?

1. Купите домен на Namecheap, Google Domains
2. В настройках хостинга добавьте Custom Domain
3. Обновите DNS записи:
   - Type: `CNAME`
   - Name: `@` или `www`
   - Value: ваш хостинг URL

### HTTPS не работает, что делать?

Большинство платформ (Render, Railway) автоматически выдают SSL сертификаты. Если нет:
1. Подождите 24-48 часов после настройки домена
2. Используйте Let's Encrypt (бесплатно)
3. Cloudflare для SSL и CDN

### Как ускорить загрузку сайта?

1. Используйте CDN для статических файлов
2. Минифицируйте CSS/JS
3. Включите кэширование в Flask
4. Используйте Cloudflare

---

## Генерация файлов

### Для чего нужен sitemap.xml?

Sitemap помогает поисковым системам (Google, Yandex) индексировать ваш сайт. Генерируйте после добавления проектов:

```
http://localhost:5000/generate-sitemap
```

### Как использовать PROJECTS_OVERVIEW.md?

Это автоматически генерируемый README со всеми проектами. Можно:
1. Добавить в корень репозитория
2. Использовать в документации
3. Отправлять работодателям

Генерация:
```
http://localhost:5000/generate-readme
```

### Можно ли экспортировать проекты в Excel?

Да! Используйте скрипт:

```bash
python export_projects.py
```

Будет создан CSV файл, который можно открыть в Excel.

---

## API

### Как получить список проектов через API?

```bash
curl http://localhost:5000/api/projects
```

Или откройте в браузере: `http://localhost:5000/api/projects`

### Можно ли использовать API с другого сайта?

Да, но нужно настроить CORS. Добавьте в `requirements.txt`:
```
flask-cors==4.0.0
```

В `app.py`:
```python
from flask_cors import CORS
CORS(app)
```

Подробнее: `API.md`

---

## Безопасность

### Нужно ли менять SECRET_KEY?

Да! В продакшне обязательно установите переменную окружения:

```bash
export SECRET_KEY="your-random-secret-key"
```

Или измените в `config.py` (не рекомендуется для публичных репозиториев).

### Безопасно ли публиковать портфолио?

Да, если вы:
1. Не храните секретные данные в `info.json`
2. Используете безопасный `SECRET_KEY`
3. Не публикуете личную информацию в открытом доступе

---

## Разработка и расширение

### Как добавить новые поля в проекты?

1. Добавьте поле в `info.json`:
```json
{
  "name": "Project",
  "github_stars": 150,
  "demo_url": "https://demo.com"
}
```

2. Модифицируйте `templates/index.html`:
```html
<p>⭐ Stars: {{ project.github_stars }}</p>
<a href="{{ project.demo_url }}">Demo</a>
```

### Можно ли добавить базу данных?

Да! Вместо JSON файлов можно использовать SQLite, PostgreSQL:

```python
from flask_sqlalchemy import SQLAlchemy

app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///projects.db'
db = SQLAlchemy(app)

class Project(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(100))
    # ...
```

### Как добавить админ-панель?

Используйте Flask-Admin:

```bash
pip install flask-admin
```

```python
from flask_admin import Admin

admin = Admin(app, name='PortfolioHub Admin')
# Добавьте модели
```

### Можно ли добавить аналитику?

Да! Добавьте Google Analytics в `templates/base.html`:

```html
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

---

## Устранение проблем

### Ошибка "Module not found"

```bash
pip install -r requirements.txt
```

### Ошибка "Port already in use"

Измените порт в `app.py` или завершите процесс:

**Windows:**
```bash
netstat -ano | findstr :5000
taskkill /PID <PID> /F
```

**Linux/Mac:**
```bash
lsof -i :5000
kill -9 <PID>
```

### Страница не обновляется

1. Очистите кэш: `Ctrl+Shift+R` (Chrome) или `Ctrl+F5`
2. Перезапустите приложение
3. Проверьте режим разработки: `FLASK_ENV=development`

### Русские буквы отображаются неправильно

Убедитесь, что файлы сохранены в UTF-8:
- VS Code: внизу справа выбрать UTF-8
- Notepad++: Encoding → UTF-8
- Sublime Text: File → Save with Encoding → UTF-8

---

## Полезные ресурсы

- 📘 [Официальная документация Flask](https://flask.palletsprojects.com/)
- 🎨 [Bootstrap 5 документация](https://getbootstrap.com/docs/5.3/)
- 🔍 [JSON валидатор](https://jsonlint.com/)
- 🎨 [Генератор градиентов](https://cssgradient.io/)
- 🎨 [Генератор цветовых палитр](https://coolors.co/)
- 🔤 [Google Fonts](https://fonts.google.com/)

---

## Остались вопросы?

Создайте Issue на GitHub или свяжитесь с автором!

**Последнее обновление:** 2025-11-02

