# 🚀 Быстрый старт PortfolioHub

## ⚡ За 3 минуты

### Windows

1. **Двойной клик на `run.bat`** - всё настроится автоматически!
2. Откройте браузер: `http://localhost:5000`
3. Готово! 🎉

### Linux/Mac

```bash
chmod +x run.sh
./run.sh
```

---

## 📝 Добавление проекта

### Способ 1: Интерактивный скрипт (рекомендуется)

```bash
python add_project.py
```

Следуйте инструкциям в терминале.

### Способ 2: Вручную

1. Создайте папку: `projects/my-project/`
2. Создайте `info.json`:

```json
{
  "name": "Мой Проект",
  "description": "Описание проекта",
  "tags": ["python", "flask"],
  "link": "https://github.com/username/repo"
}
```

3. Обновите страницу - проект появится!

---

## 🎨 Настройка информации о себе

Откройте `config.py` и измените:

```python
AUTHOR_INFO = {
    'name': 'Ваше Имя',
    'role': 'Python Developer',
    'description': 'Ваше описание',
    'contacts': {
        'github': 'https://github.com/username',
        'email': 'email@example.com',
        'telegram': '@username'
    }
}
```

---

## 🌐 Деплой в один клик

### Render.com (бесплатно)

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Нажмите **New** → **Web Service**
3. Подключите GitHub репозиторий
4. Render автоматически обнаружит Flask и задеплоит!

### Railway.app (бесплатно)

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. **New Project** → **Deploy from GitHub repo**
3. Выберите репозиторий
4. Готово!

### Heroku

```bash
heroku create my-portfolio
git push heroku main
```

---

## 🔧 Полезные команды

### Запуск приложения
```bash
python app.py
```

### Мониторинг изменений
```bash
python watcher.py
```

### Генерация sitemap
Откройте: `http://localhost:5000/generate-sitemap`

### Генерация README проектов
Откройте: `http://localhost:5000/generate-readme`

### API проектов (JSON)
Откройте: `http://localhost:5000/api/projects`

---

## 💡 Советы

### Красивые теги

Используйте понятные названия:
- ✅ `python`, `flask`, `api`, `telegram-bot`
- ❌ `py`, `flsk`, `tg-bot`

### Качественные описания

Хорошее описание:
> "Телеграм-бот для автоматизации задач с интеграцией OpenAI API и поддержкой команд"

Плохое описание:
> "Бот"

### Структура проекта

```
projects/
├── ai-notes-bot/
│   ├── info.json          # Обязательно!
│   ├── README.md          # Опционально
│   └── screenshots/       # Опционально
├── web-scraper/
│   └── info.json
└── ...
```

---

## 🐛 Проблемы?

### Порт занят
Измените порт в `app.py`:
```python
app.run(debug=True, host='0.0.0.0', port=8000)
```

### Проект не отображается
1. Проверьте, что `info.json` валидный (используйте [jsonlint.com](https://jsonlint.com))
2. Убедитесь, что файл называется точно `info.json`
3. Перезапустите приложение

### Ошибка импорта модулей
```bash
pip install -r requirements.txt
```

---

## 📚 Документация

Полная документация в файле `README.md`

---

**Удачи! 🚀**

